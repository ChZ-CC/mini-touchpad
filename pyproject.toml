[project]
name = "touch-pad"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cryptography>=48.0.0",
    "pyautogui>=0.9.54",
    "pynput>=1.8.2",
    "python-dotenv>=1.2.2",
    "websockets>=16.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
